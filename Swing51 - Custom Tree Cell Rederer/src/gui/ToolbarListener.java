package gui;

public interface ToolbarListener {
	
	// To change name and all references to it simultaneously, select the text, right click, refactor, rename
	public void saveEventOccured();
	public void refreshEventOccured();
}
